# simple-port-scanner
Simple Port Scanner A basic Python script that scans a specified range of ports on a given host (IP address or domain). It checks which ports are open and reports the status, using Python's built-in socket and threading libraries for faster scanning.
